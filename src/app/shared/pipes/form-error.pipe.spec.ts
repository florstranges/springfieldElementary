import { FormErrorPipe } from './form-error.pipe';

describe('FormErrorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
